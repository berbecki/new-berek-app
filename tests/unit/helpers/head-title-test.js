import { headTitle } from '../../../helpers/head-title';
import { module, test } from 'qunit';

module('Unit | Helper | head title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = headTitle(42);
  assert.ok(result);
});
